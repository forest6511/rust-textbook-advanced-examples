/target
**/*.rs.bk
*.pdb
.DS_Store
.idea/
.vscode/
flamegraph.svg
perf.data*
*.profraw
dhat-heap.json
dhat-ad-hoc.json

# Allow Cargo.lock at workspace root (binaries) but ignore in subdirectories if needed
